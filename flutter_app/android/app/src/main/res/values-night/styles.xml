<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on.

         Parented to a MaterialComponents theme for the same reason as the light
         variant: flutter_stripe's sheets require an AppCompat-derived theme. -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Req 5.5: this is the one that mattered. Under a dark system theme the
             scaffold's ?android:colorBackground resolved to this dark parent's grey,
             so the window went dark and then swapped to the app's light first frame -
             main.dart passes no darkTheme, so there is no dark first frame to match.
             @color/splash_background is AppColors.background in values-night too. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
