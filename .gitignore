# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
# Scratch build/dev output, for running `next build` or a second `next dev` while
# another dev server holds `.next`. See the `distDir` note in next.config.ts.
/.next-*/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# local MCP config (holds provider secret keys)
.kiro/settings/mcp.json

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
.env*

# playwright / e2e
/test-results/
/playwright-report/
/playwright/.auth/
/ux-review/captures/
/blob-report/

# Scratch dump artifact, regenerated locally
copy-all.txt
