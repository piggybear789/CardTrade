<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off.

         Parented to a MaterialComponents theme rather than the framework's
         Theme.Light.NoTitleBar because flutter_stripe inflates AppCompat views
         in its payment sheets, which require an AppCompat-derived theme on the
         hosting activity. -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Req 5.5: the same colour the splash sits on and the same colour the first
             Flutter frame paints (AppColors.background). The scaffold used
             ?android:colorBackground, which is the theme's own background rather than
             the app's; matching the app is what removes the flash between the launch
             window, this window and the first frame. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
